# Vendored upstream sources — kept byte-faithful to HeroUI.
src/demos/

# Generated by scripts/*.mjs; formatting them creates churn on every regen.
src/data/

# Hand-written docs. oxfmt rewrites emphasis, re-pads tables and re-wraps
# fenced blocks, which is destructive to deliberately formatted prose.
*.md

# Build output, research scratch, and copied binary assets.
.next/
.research/
public/shots/

# Generated on the Rust side (wasm-bindgen glue and the built module), plus
# its hand-written hosting page kept byte-faithful in the wasm worktree.
public/gallery/
