## Summary

<!-- What does this change and why? One pull request, one thing. -->

## Public API

<!--
Required. List every public item added, changed or removed, grouped by crate,
with its signature and one line on its purpose. Write "None." if nothing public
changed. See CONTRIBUTING.md.

### herogpui-components
- `pub fn is_disabled(self, bool) -> Self` on `Foo` — disables the control.
-->

## Parity

<!-- HeroUI v3.2.6 source this follows, or "HeroGPUI extension" with the audit entries it adds. -->

## Verification

<!-- Commands run (fmt, clippy, run-tests.sh, audits, extract:check) and anything not run, with why. -->
